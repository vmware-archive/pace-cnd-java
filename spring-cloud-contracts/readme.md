# Spring Cloud Contracts Demo

1. From the `spring-cloud-contract` directory, execute

```
mvn clean install -DskipTests
```

2. Run the LoanApprovalTests JUnit tests in the `spring-cloud-contract-client`.
